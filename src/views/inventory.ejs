<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Envanter - <%= guild.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .inventory-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 1rem;
        }
        
        .item-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 12px;
            padding: 1.5rem;
            text-align: center;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }
        
        .item-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }
        
        .item-card.common { border-color: #9ca3af; }
        .item-card.rare { border-color: #3b82f6; }
        .item-card.epic { border-color: #8b5cf6; }
        .item-card.legendary { border-color: #f59e0b; }
        
        .item-icon {
            font-size: 3rem;
            margin-bottom: 0.5rem;
        }
        
        .rarity-badge {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            margin-bottom: 0.5rem;
        }
        
        .rarity-common { background-color: #6b7280; color: white; }
        .rarity-rare { background-color: #3b82f6; color: white; }
        .rarity-epic { background-color: #8b5cf6; color: white; }
        .rarity-legendary { background-color: #f59e0b; color: white; }
        
        .stats-card {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            border-radius: 12px;
            padding: 2rem;
            text-align: center;
            color: white;
            margin-bottom: 2rem;
        }
        
        .empty-inventory {
            text-align: center;
            padding: 4rem 2rem;
            color: #6b7280;
        }
        
        .empty-inventory-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body class="bg-gray-900 text-white">
    <%- include('../partials/navbar') %>
    
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-6xl mx-auto">
            <!-- Kullanıcı İstatistikleri -->
            <div class="stats-card">
                <h1 class="text-3xl font-bold mb-4">
                    <span class="mr-3">🎒</span>
                    <%= user.username %>'in Envanteri
                </h1>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                    <div class="bg-white bg-opacity-20 rounded-lg p-4">
                        <div class="text-2xl font-bold"><%= userCoins.toLocaleString() %></div>
                        <div class="text-sm opacity-80">💰 Coin</div>
                    </div>
                    <div class="bg-white bg-opacity-20 rounded-lg p-4">
                        <div class="text-2xl font-bold"><%= userXP.toLocaleString() %></div>
                        <div class="text-sm opacity-80">⭐ XP</div>
                    </div>
                    <div class="bg-white bg-opacity-20 rounded-lg p-4">
                        <div class="text-2xl font-bold"><%= inventory ? inventory.totalValue.toLocaleString() : 0 %></div>
                        <div class="text-sm opacity-80">💎 Envanter Değeri</div>
                    </div>
                </div>
            </div>

            <!-- Envanter İçeriği -->
            <div class="bg-gray-800 rounded-lg p-6">
                <h2 class="text-2xl font-bold mb-6 flex items-center">
                    <span class="mr-3">📦</span>
                    Eşyalarım
                    <span class="ml-auto text-sm font-normal text-gray-400">
                        <%= inventory ? inventory.items.length : 0 %> eşya
                    </span>
                </h2>

                <% if (!inventory || inventory.items.length === 0) { %>
                    <div class="empty-inventory">
                        <div class="empty-inventory-icon">📭</div>
                        <h3 class="text-xl font-semibold mb-2">Envanterin Boş</h3>
                        <p class="text-gray-400 mb-4">Oyunları oynayarak eşya kazanmaya başla!</p>
                        <a href="/games" class="inline-block bg-blue-600 hover:bg-blue-700 px-6 py-3 rounded-lg font-semibold transition-colors">
                            Oyunlara Git
                        </a>
                    </div>
                <% } else { %>
                    <div class="inventory-grid">
                        <% inventory.items.forEach(item => { %>
                            <div class="item-card <%= item.rarity %>">
                                <div class="item-icon"><%= item.icon || '📦' %></div>
                                <div class="rarity-badge rarity-<%= item.rarity %>"><%= item.rarity %></div>
                                <h3 class="font-bold text-lg mb-2"><%= item.name %></h3>
                                <p class="text-sm text-gray-300 mb-3">Miktar: <%= item.quantity %></p>
                                <div class="text-yellow-400 font-semibold">
                                    💰 <%= (item.value * item.quantity).toLocaleString() %> Coin
                                </div>
                                <div class="text-xs text-gray-400 mt-2">
                                    <%= new Date(item.obtainedAt).toLocaleDateString('tr-TR') %>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                <% } %>
            </div>

            <!-- Hızlı Eylemler -->
            <div class="mt-8 grid grid-cols-1 md:grid-cols-2 gap-4">
                <a href="/games" class="bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 rounded-lg p-6 text-center transition-all transform hover:scale-105">
                    <div class="text-3xl mb-2">🎮</div>
                    <h3 class="text-xl font-bold mb-2">Oyunlara Dön</h3>
                    <p class="text-gray-200">Daha fazla eşya kazanmak için oyunları oyna</p>
                </a>
                
                <div class="bg-gradient-to-r from-green-600 to-teal-600 rounded-lg p-6 text-center">
                    <div class="text-3xl mb-2">📊</div>
                    <h3 class="text-xl font-bold mb-2">İstatistikler</h3>
                    <p class="text-gray-200">Toplam <%= inventory ? inventory.items.length : 0 %> farklı eşya</p>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>